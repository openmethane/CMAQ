Stop passing gfortran-only flags (`-fbacktrace`, `-fallow-argument-mismatch`) to the C compiler when building I/O API, which produced a "valid for Fortran but not for C" warning for every C source file.
