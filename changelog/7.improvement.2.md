Populate `OPENMETHANE_CMAQ_VERSION` from a build `ARG` supplied by CI from `pyproject.toml`, instead of a hardcoded value that was never bumped. Local builds default to `development`, and release builds are checked against the git tag.
